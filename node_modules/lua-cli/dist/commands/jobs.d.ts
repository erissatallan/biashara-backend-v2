/**
 * Jobs Command
 * Manages agent scheduled jobs for sandbox and production environments
 */
/**
 * Main jobs command - manages agent jobs.
 *
 * Supports both interactive and non-interactive modes:
 * - Interactive: prompts for action and job selection
 * - Non-interactive: use action argument with -i and -v flags
 */
export declare function jobsCommand(action?: string, cmdObj?: any): Promise<void>;
//# sourceMappingURL=jobs.d.ts.map