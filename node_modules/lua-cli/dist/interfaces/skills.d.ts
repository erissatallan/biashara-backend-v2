/**
 * Skills Interfaces
 * Structures for skill management
 */
/**
 * Tool definition within a skill version
 */
export interface SkillTool {
    name: string;
    description: string;
}
/**
 * Skill version information
 */
export interface SkillVersion {
    id: string;
    version: string;
    description: string;
    active: boolean;
    context: string;
    tools: SkillTool[];
}
/**
 * Skill source - indicates how the skill was added to the agent
 */
export type SkillSource = 'cli' | 'marketplace' | 'manual';
/**
 * Skill information
 */
export interface Skill {
    id: string;
    name: string;
    description: string;
    public: boolean;
    active: boolean;
    source?: SkillSource;
    createdAt: string;
    updatedAt: string;
    versions: SkillVersion[];
}
/**
 * Response from get all skills API endpoint
 */
export interface GetSkillsResponse {
    skills: Skill[];
}
/**
 * Response from delete skill API endpoint
 * If deleted is false and deactivated is true, the skill has versions
 * and cannot be deleted, so it was deactivated instead
 */
export interface DeleteSkillResponse {
    message: string;
    deleted: boolean;
    deactivated: boolean;
}
/**
 * Request to create a new skill
 */
export interface CreateSkillRequest {
    name: string;
    description?: string;
    context?: string;
}
/**
 * Response from create skill API endpoint
 */
export interface CreateSkillResponse {
    id: string;
    name: string;
    description?: string;
    agentId: string;
    context?: string;
}
/**
 * Tool definition for skill version
 */
export interface SkillVersionToolInput {
    name: string;
    code: string;
    executeFunction: string;
    inputSchema: string;
    description?: string;
}
/**
 * Request to push a skill version
 */
export interface PushSkillVersionRequest {
    name: string;
    version: string;
    skillId: string;
    description?: string;
    context?: string;
    code: string;
    executeFunction: string;
    tools: SkillVersionToolInput[];
}
/**
 * Response from get skill versions API endpoint
 * Uses VersionInfo which has createdDate, createdBy, etc. fields
 */
export interface GetSkillVersionsResponse {
    versions: SkillVersionInfo[];
    activeVersionId?: string;
}
/**
 * Extended skill version info returned from versions API
 * Includes audit fields for version selection in deploy
 */
export interface SkillVersionInfo {
    version: string;
    createdDate: string;
    createdBy: string;
    isCurrent: boolean;
    createdByEmail: string;
    createdByFullName: string;
}
//# sourceMappingURL=skills.d.ts.map