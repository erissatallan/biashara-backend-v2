/**
 * MCP Server Interfaces
 * API request/response types for MCP server management
 *
 * Note: stdio transport is not supported yet in lua-cli.
 * Use 'streamable-http' (recommended) or 'sse' (legacy) instead.
 */
/**
 * Source/origin of an MCP server configuration.
 * Extensible type for tracking where MCP servers come from.
 */
export type MCPServerSource = 'manual' | 'unifiedto';
/**
 * Base MCP Server response fields
 */
interface MCPServerResponseBase {
    id: string;
    name: string;
    agentId: string;
    active: boolean;
    /** Source/origin of the MCP server (manual, unifiedto, etc.) */
    source?: MCPServerSource;
    timeout?: number;
    createdAt: string;
    updatedAt: string;
}
/**
 * MCP Server response for SSE transport (legacy)
 */
export interface MCPServerSSEResponse extends MCPServerResponseBase {
    transport: 'sse';
    /** Static URL */
    url?: string;
    /** Compressed function code for runtime URL resolution using env() API */
    urlResolver?: string;
    /** Static headers */
    headers?: Record<string, string>;
    /** Compressed function code for runtime headers resolution using env() API */
    headersResolver?: string;
}
/**
 * MCP Server response for Streamable HTTP transport (recommended)
 */
export interface MCPServerStreamableHttpResponse extends MCPServerResponseBase {
    transport: 'streamable-http';
    /** Static URL */
    url?: string;
    /** Compressed function code for runtime URL resolution using env() API */
    urlResolver?: string;
    /** Static headers */
    headers?: Record<string, string>;
    /** Compressed function code for runtime headers resolution using env() API */
    headersResolver?: string;
}
/**
 * Union type for MCP Server API responses
 */
export type MCPServerResponse = MCPServerSSEResponse | MCPServerStreamableHttpResponse;
/**
 * Request payload for creating an MCP server
 */
export interface CreateMCPServerRequest {
    name: string;
    transport: 'sse' | 'streamable-http';
    /** Static URL */
    url?: string;
    /** Compressed function code for runtime URL resolution using env() API */
    urlResolver?: string;
    /** Static headers */
    headers?: Record<string, string>;
    /** Compressed function code for runtime headers resolution using env() API */
    headersResolver?: string;
    timeout?: number;
    /** Source/origin of the MCP server (manual, unifiedto, etc.) */
    source?: MCPServerSource;
}
/**
 * Request payload for updating an MCP server
 */
export interface UpdateMCPServerRequest {
    name?: string;
    transport?: 'sse' | 'streamable-http';
    /** Static URL */
    url?: string;
    /** Compressed function code for runtime URL resolution using env() API */
    urlResolver?: string;
    /** Static headers */
    headers?: Record<string, string>;
    /** Compressed function code for runtime headers resolution using env() API */
    headersResolver?: string;
    timeout?: number;
    /** Source/origin of the MCP server (manual, unifiedto, etc.) */
    source?: MCPServerSource;
}
export {};
//# sourceMappingURL=mcp.d.ts.map