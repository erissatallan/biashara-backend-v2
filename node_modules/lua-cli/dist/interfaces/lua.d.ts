/**
 * Lua Runtime Interfaces
 * Request-level runtime information available in the execution sandbox
 */
/**
 * Channel type.
 * Represents the communication channel through which a request originated.
 * Known channels are typed as string literals, unrecognized channels are unknown.
 */
export type Channel = 'web' | 'whatsapp' | 'facebook' | 'instagram' | 'slack' | 'teams' | 'front' | 'messagebird' | 'api' | 'dev' | 'email' | string;
/**
 * Webhook request information from channel integrations.
 * Contains the raw webhook payload from the channel provider.
 */
export interface WebhookRequest {
    /** The raw, unmodified webhook payload from the channel provider */
    payload: any;
}
/**
 * Lua request context.
 * Contains information about the current request.
 */
export interface LuaRequest {
    /** The channel through which the request originated */
    channel: Channel;
    /** Webhook data from channel integrations (only present for webhook-based channels) */
    webhook?: WebhookRequest;
}
/**
 * Lua runtime API.
 * Provides access to request-level runtime information.
 */
export interface LuaRuntime {
    /** Request context information */
    request: LuaRequest;
}
//# sourceMappingURL=lua.d.ts.map