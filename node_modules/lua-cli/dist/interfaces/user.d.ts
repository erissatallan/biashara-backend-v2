/**
 * Immutable user profile information from Lua's user database
 */
export interface ImmutableUserProfile {
    userId: string;
    fullName: string;
    mobileNumbers: string[];
    emailAddresses: string[];
}
/**
 * Base user agent data structure
 */
export interface UserAgentData extends Record<string, any> {
    /**
     * @deprecated Use `user._luaProfile.userId` instead. This property is kept for backwards compatibility and will be removed in a future version.
     * Note: This refers only to the Lua userId. If you use a custom `userId` property in your app that is not the Lua user ID, ignore this deprecation notice.
     */
    userId?: string;
}
/**
 * Profile response from user lookup endpoints
 */
export interface ProfileResponse {
    id: string;
    fullName: string;
    mobileNumbers: Array<{
        number: string;
        validated: boolean;
        validatedAt: number;
    }>;
    emailAddresses: Array<{
        address: string;
        validated: boolean;
        validatedAt: number;
    }>;
    country?: {
        code?: string;
        name?: string;
    };
}
/**
 * Options for looking up a user by email or phone
 */
export interface UserLookupOptions {
    /** Email address to look up */
    email?: string;
    /** Phone number to look up (with or without + prefix) */
    phone?: string;
}
/**
 * User data response from the API
 * Includes the user's data and optional Lua profile
 */
export interface UserDataResponse {
    _luaProfile?: ImmutableUserProfile;
    [key: string]: any;
}
/**
 * Admin user response from the API
 */
export interface AdminUserResponse {
    uid: string;
    email?: string;
    displayName?: string;
    [key: string]: any;
}
/**
 * Response from sending a message
 */
export interface SendMessageResponse {
    success: boolean;
    messageId?: string;
    [key: string]: any;
}
//# sourceMappingURL=user.d.ts.map