/**
 * Sandbox Service
 * Handles all API calls for sandbox/dev mode (chat, push, update).
 * Moved from utils/dev-api.ts — this is a service layer, not a utility.
 */
import { DevVersionResponse, UpdateDevVersionResponse, SandboxIdsMap } from '../interfaces/dev.js';
import { type CompilationManifest } from '../compiler/types.js';
import { YamlConfig } from '../types/yaml.types.js';
/**
 * Sends a chat message to the API with skill and processor overrides for sandbox testing.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param skillId - Skill ID
 * @param sandboxId - Sandbox skill ID
 * @param message - Message to send
 * @param persona - Optional persona override
 * @param yamlConfig - YAML config containing skills and processors
 * @returns The chat response text or null if failed
 */
export declare function sendChatMessage(apiKey: string, agentId: string, skillId: string, sandboxId: string, message: string, persona: string | undefined, yamlConfig: YamlConfig): Promise<string | null>;
/**
 * Updates an existing sandbox version.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param skillId - Skill ID
 * @param sandboxVersionId - Existing sandbox version ID
 * @param versionData - Version data to update
 * @returns Update response
 */
export declare function updateDevVersion(apiKey: string, agentId: string, skillId: string, sandboxVersionId: string, versionData: any): Promise<UpdateDevVersionResponse>;
/**
 * Creates a new sandbox version.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param skillId - Skill ID
 * @param versionData - Version data to create
 * @returns Dev version response
 */
export declare function pushDevVersion(apiKey: string, agentId: string, skillId: string, versionData: any): Promise<DevVersionResponse>;
/**
 * Pushes a single skill to the sandbox.
 * Attempts to update existing sandbox version, creates new one if update fails.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param skillId - Skill ID
 * @param skillData - Skill data to push
 * @param isInitial - Whether this is the initial push (affects logging)
 * @returns True if successful, false otherwise
 */
export declare function pushSingleSkillToSandbox(apiKey: string, agentId: string, skillId: string, skillData: any, isInitial?: boolean): Promise<boolean>;
/**
 * Pushes multiple skills to the sandbox using manifest format.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param manifest - Compilation manifest
 * @param yamlConfig - YAML config with skill IDs
 * @param isInitial - Whether this is the initial push
 * @returns Map of skill names to their sandbox IDs
 */
export declare function pushSkillsToSandbox(apiKey: string, agentId: string, manifest: CompilationManifest, yamlConfig: YamlConfig, isInitial?: boolean): Promise<SandboxIdsMap>;
/**
 * Pushes a single preprocessor to the sandbox.
 * Attempts to update existing sandbox version, creates new one if update fails.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param preprocessorId - PreProcessor ID
 * @param preprocessorData - PreProcessor data to push
 * @param isInitial - Whether this is the initial push (affects logging)
 * @returns True if successful, false otherwise
 */
export declare function pushSinglePreProcessorToSandbox(apiKey: string, agentId: string, preprocessorId: string, preprocessorData: any, isInitial?: boolean): Promise<boolean>;
/**
 * Pushes a single postprocessor to the sandbox.
 * Attempts to update existing sandbox version, creates new one if update fails.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param postprocessorId - PostProcessor ID
 * @param postprocessorData - PostProcessor data to push
 * @param isInitial - Whether this is the initial push (affects logging)
 * @returns True if successful, false otherwise
 */
export declare function pushSinglePostProcessorToSandbox(apiKey: string, agentId: string, postprocessorId: string, postprocessorData: any, isInitial?: boolean): Promise<boolean>;
/**
 * Pushes preprocessors and postprocessors to sandbox using manifest format.
 *
 * @param apiKey - User's API key
 * @param agentId - Agent ID
 * @param manifest - Compilation manifest
 * @param yamlConfig - YAML config with processor IDs
 * @param isInitial - Whether this is the initial push
 * @returns Object with counts of pushed processors
 */
export declare function pushProcessorsToSandbox(apiKey: string, agentId: string, manifest: CompilationManifest, yamlConfig: YamlConfig, isInitial?: boolean): Promise<{
    preprocessors: number;
    postprocessors: number;
}>;
//# sourceMappingURL=sandbox.service.d.ts.map