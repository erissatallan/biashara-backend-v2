/**
 * Webhook Handler
 *
 * Handles CLI operations for webhook primitives.
 *
 * Webhooks:
 * - Use 'active' field on entity
 * - Use 'isActive' field on versions
 */
import { PrimitiveKind, ManifestPrimitive } from '../compiler/types.js';
import WebhookApi from '../api/webhook.api.service.js';
import type { PushWebhookVersionDTO } from '../interfaces/webhooks.js';
import { BaseVersionedHandler } from './base.handler.js';
import type { YamlPrimitiveConfig } from './types.js';
import type { YamlConfigWebhook } from '../types/yaml.types.js';
/**
 * Webhook primitive handler
 */
export declare class WebhookHandler extends BaseVersionedHandler<YamlConfigWebhook, PushWebhookVersionDTO> {
    readonly kind = PrimitiveKind.WEBHOOK;
    readonly displayName = "webhook";
    readonly displayNamePlural = "webhooks";
    readonly deleteCommand = "lua webhooks delete";
    readonly yamlConfig: YamlPrimitiveConfig;
    protected getApi(apiKey: string, agentId: string): WebhookApi;
    protected fetchFromServer(api: WebhookApi): Promise<any[] | null>;
    protected createOnServer(api: WebhookApi, primitive: ManifestPrimitive): Promise<string | null>;
    /**
     * Webhooks use 'active' field directly on the entity.
     */
    isActive(serverItem: any): boolean;
    pushToServer(apiKey: string, agentId: string, entityId: string, pushData: PushWebhookVersionDTO): Promise<{
        success: boolean;
        error: string | undefined;
    }>;
    publishVersion(apiKey: string, agentId: string, entityId: string, version: string): Promise<{
        success: boolean;
        error: string | undefined;
    }>;
    /**
     * Override to include webhook schemas in push data.
     */
    protected buildPushData(primitive: ManifestPrimitive, compressedCode: string): Record<string, unknown>;
}
export declare const webhookHandler: WebhookHandler;
//# sourceMappingURL=webhook.handler.d.ts.map